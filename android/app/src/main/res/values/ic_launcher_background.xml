<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Matches the dark theme background colour (hex 0A0E17) from
         src/index.css so the launcher icon's backdrop reads as
         "same dark canvas the app launches into" rather than the
         default Capacitor white. The Oceans Symphony wave/glow
         logo is built to sit on a dark backdrop. -->
    <color name="ic_launcher_background">#0A0E17</color>
</resources>